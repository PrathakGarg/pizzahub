import { HeaderContainer } from "./header.styles";

const Header = () => {
    return (
        <HeaderContainer>
            <h1>Pizza Hub</h1>
        </HeaderContainer>
    );
};

export default Header;